
    // Create Dino Constructor
 function Dinosaur(species, diet, weight, height, where, when, fact, image)  {
    this.species = species;
    this.diet = diet;
    this.weight =  weight;
    this.height = height;
    this.where = where;
    this.when = when;
    this.fact = fact;
this.image = image;
    }


    // human constructor function

    // function Dinosaur (species, weight, diet) 
    // {
        //     this.species = species;
        //     this.weight = weight;
        //     this.diet = diet
        //     this.fact = function () {
            //         console.log(`A ${this.species} likes to eat ${this.diet}`)
            //     };
            
            // }
            
            let tRex = new Dinosaur();
            
            console.log(tRex);
            // Create Dino Objects
            
            
            // Create Human Object
            
            function Human(species, personName, diet, weight, feet, inches, image )  {
                this.species = species;
                this.name = personName;
                this.diet = diet;
                this.weight = weight;
                this.feet = feet;
                this.inches = inches;
                this.image = image;
            }


            let brian = new Human();

            console.log(brian);
            // Use IIFE to get human data from form

            const name = getElementById('name');
            const feet = getElementById('feet');
            const inches = getElementById('inches');
            const weight = getElementById('weight');
            const diet = getElementById('diet');
            const button = getElementById('btn');

            const grid = getElementById('grid');


            forEach(el => {

            })
            const 
            
            (function () {
                document.getElementById('btn');

            })()
            // Create Dino Compare Method 1
    // NOTE: Weight in JSON file is in lbs, height in inches. 
// else if statement
    
    // Create Dino Compare Method 2
    // NOTE: Weight in JSON file is in lbs, height in inches.
// else if statement

    
    // Create Dino Compare Method 3
    // NOTE: Weight in JSON file is in lbs, height in inches.
// else if statement


    // Generate Tiles for each Dino in Array
  
        // Add tiles to DOM

    // Remove form from screen
// remove method

// On button click, prepare and display infographic
